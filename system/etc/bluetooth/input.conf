# Configuration file for the input service

# This section contains options which are not specific to any
# particular interface
[General]

# Set idle timeout (in seconds) before the connection will be disconnect and
# the input device is removed.
# Defaults: 0 (disabled)
#IdleTimeout=0

# Enable HID protocol handling in userspace input profile
# Possible values:
#  - persist: Use UHID in persistent mode (keyboard only)
#  - true: Use UHID instead
#  - false: User kernel HIDP
# Defaults to true
#UserspaceHID=true

# NOTE (2026-09-14): set to false for the Wacom Intuos BT S (056A:0377).
# That tablet pairs but never bonds (Paired: yes / Bonded: no), so with the
# BlueZ >=5.51 default of true, bluetoothd logs
#   hidp_add_connection() Rejected connection from !bonded device
# and the HID profile never comes up, so the kernel wacom driver never binds.
# Unbonded HID is still gated by the device being Trusted.
# Limit HID connections to bonded devices
# The HID Profile does not specify that devices must be bonded, however some
# platforms may want to make sure that input connections only come from bonded
# device connections. Several older mice have been known for not supporting
# pairing/encryption.
# Defaults to true for security.
ClassicBondedOnly=false

# LE upgrade security
# Enables upgrades of security automatically if required.
# Defaults to true to maximize device compatibility.
#LEAutoSecurity=true
